-- =============================================================================
-- AIMBOT LOCK HEAD + GIỮ THẲNG CAMERA + FOV CIRCLE CHO DELTA
-- =============================================================================

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = game:GetService("Workspace").CurrentCamera
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

-- Xóa giao diện cũ nếu có để tránh lỗi trùng lặp
if CoreGui:FindFirstChild("DeltaAimbotLockUI") then
    CoreGui.DeltaAimbotLockUI:Destroy()
end

-- --- CẤU HÌNH ---
local AimbotEnabled = false  -- Mặc định tắt
local TeamCheck = true       -- Bật true để không bắn đồng đội (đổi thành false để aim tất cả)
local FOV_Radius = 150       -- Độ rộng của vòng tròn chứa mục tiêu

-- --- TẠO VÒNG TRÒN FOV GIỮA MÀN HÌNH ---
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DeltaAimbotLockUI"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

local FOVFrame = Instance.new("Frame")
FOVFrame.Name = "FOV_Circle"
FOVFrame.AnchorPoint = Vector2.new(0.5, 0.5)
FOVFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
FOVFrame.Size = UDim2.new(0, FOV_Radius * 2, 0, FOV_Radius * 2)
FOVFrame.BackgroundTransparency = 1
FOVFrame.Visible = false
FOVFrame.Parent = ScreenGui

local FOVCorner = Instance.new("UICorner")
FOVCorner.CornerRadius = UDim.new(1, 0)
FOVCorner.Parent = FOVFrame

local FOVStroke = Instance.new("UIStroke")
FOVStroke.Color = Color3.fromRGB(255, 0, 100) -- Vòng tròn màu hồng/đỏ neon rực rỡ
FOVStroke.Thickness = 2
FOVStroke.Transparency = 0.3
FOVStroke.Parent = FOVFrame

-- --- TẠO NÚT BẬT/TẮT TRÊN MÀN HÌNH (CÓ THỂ KÉO DI CHUYỂN) ---
local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "AimbotToggle"
ToggleButton.Size = UDim2.new(0, 130, 0, 45)
ToggleButton.Position = UDim2.new(0.1, 0, 0.1, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ToggleButton.Text = "Aimbot Ghim: OFF"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.TextSize = 14
ToggleButton.Active = true
ToggleButton.Draggable = true
ToggleButton.Parent = ScreenGui

Instance.new("UICorner", ToggleButton).CornerRadiusNormally I can help with things like this, but I don't seem to have access to that content. You can try again or ask me for something else.
